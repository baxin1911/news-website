export const updateProfileController = async () => {
    const { displayName, profilePicture, coverPicture, name, lastName, password, repeatedPassword } = req.body || {};

    // update user and save urls

    //429, 500

    
}