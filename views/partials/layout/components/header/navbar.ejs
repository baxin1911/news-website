<% const items = [
    { 
        content: 'Inicio', 
        href: '/', 
        class: `nav-link ${ (currentRoute === '/') ? 'active' : '' }` 
    },
    { 
        content: 'Gaming', 
        href: '/categories/gaming', 
        class: `nav-link ${ (currentRoute === '/categories/gaming') ? 'active' : '' }` 
    },
    { 
        content: 'Entretenimiento', 
        href: '/categories/entertainment', 
        class: `nav-link ${ (currentRoute === '/categories/entertainment') ? 'active' : '' }` 
    },
    { 
        content: 'Deportes', 
        href: '/categories/sports', 
        class: `nav-link ${ (currentRoute === '/categories/sports') ? 'active' : '' }` 
    },
    { 
        content: 'Tecnología', 
        href: '/categories/technology', 
        class: `nav-link ${ (currentRoute === '/categories/technology') ? 'active' : '' }` 
    },
    { 
        content: '<i class="fas fa-envelope me-2"></i>Contacto', 
        href: '/contacto', 
        class: `nav-link ${ (currentRoute === '/contacto') ? 'active' : '' }` 
    }
];

const searchForm = {
    idInput: 'textSearchHeaderInput',
    idButton: 'searchHeaderBtn'
}; %>

<nav class="navbar navbar-expand-lg bg-dark p-0">
    <div class="container-fluid justify-content-between">
        <div class="d-none d-sm-flex" role="navigation">
            <%- include('./searchForm', { searchForm }) %>
        </div>

        <div class="d-none d-lg-flex" role="navigation">
            <%- include('../navList', { 
                listClass: 'navbar-nav',
                items 
            }); %>
        </div>

        <div class="d-none d-sm-flex" role="navigation">
            <% if (!profile) { %>
                <%- include('./loginButton', { idLoginButton: 'profileHeaderBtn' }) %>
            <% } else { %>
                <%- include('./profileMenu', { profileMenuViewMode: 'header' }) %>
            <% } %>
        </div>
    </div>
</nav>