<% const modal = {
    idModal: 'recoverModal',
    idModalLabel: 'recoverModalLabel',
    modalTitle: 'Recuperar Contraseña'
};

const extraContent = {
    withExtraContent: false
};

const form = {
    idForm: 'recoverForm',
    viewMode: 'recover',
    inputs: [
        { id: 'emailRecoverInput', label: 'Correo electrónico:', name: 'email', type: 'email' }
    ],
    idModal: '#loginModal',
    linkLabel: 'Cancelar',
    buttonLabel: 'Recuperar'
}; %>

<%- include('./modal', {
    modal,
    extraContent,
    form
}); %>