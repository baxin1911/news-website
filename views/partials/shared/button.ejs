<button id="<%= button.id %>" class="<%= button.class %>" type="button" 
    <% for (let key in button.attrs) { %> 
        <%= key %>="<%= button.attrs[key] %>" 
    <% } %>
>
    <%- button.content %>
</button>