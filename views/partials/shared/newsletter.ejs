<section>
    <h5 class="title-style">Suscríbete a nuestro boletín</h5>

    <form action="/newsletter/subscribe" method="post" class="bg-style mb-4">
        <p>Recibe las últimas noticias y actualizaciones directamente en tu bandeja de entrada.</p>
        
        <label for="newsletter-email" class="form-label">Correo electrónico:</label>

        <div class="input-group">
            <input id="newsletter-email" type="email" name="email" class="form-control" 
                placeholder="Ingresa tu correo electrónico" required>
            
            <button type="submit" class="btn btn-primary">Suscribirse</button>
        </div>

        <% if (success && success.length > 0) { %>
            <div class="alert alert-success mt-3" role="alert">
                ¡Gracias por suscribirte al boletín!
            </div>
        <% } else if (error && error.length > 0) { %>
            <div class="alert alert-danger mt-3" role="alert">
                <%= error %>
            </div>
        <% } else { %>
            
        <% } %>
    </form>
</section>